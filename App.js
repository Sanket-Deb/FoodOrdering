import React from "react";
import ReactDOM from "react-dom/client";


const Heading = <h1 id="heading" className="head">Namaste Sanket</h1>

console.log(Heading);

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(Heading);